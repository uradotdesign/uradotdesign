# Directus schema snapshots live here (schema.json), written by
# `npm run schema:snapshot` and re-applied with `npm run schema:apply`.
# Commit schema.json and configuration.json to keep CMS setup reproducible.
